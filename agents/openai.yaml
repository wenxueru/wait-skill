interface:
  display_name: "Passive Wait"
  short_description: "Wait for external state and wake a Codex thread"
  default_prompt: "Use $wait-skill to wait for an external job without active model polling."
